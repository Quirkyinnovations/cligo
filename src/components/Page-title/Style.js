import Colors from '../../constants/Colors';
import { scale} from '../../Styling/Scale/Scale';
const style = {
    container:{
        marginTop:scale(25), 
        justifyContent:'flex-start',
        alignItems:'flex-end'
    },
      
};

export default style;